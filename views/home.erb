



<body id="homepage">

<div class="container title">
  <% if session[:user] == nil %>
  <h3 class="log-error"> <a class="login" href="/login">Log In</a></h3>
  <p>or</p>
  <h3>Sign up</h3>
   <%= erb :signup %>
   <div>
   <img class="hide" id="homegif" src="http://bestanimations.com/Animals/Mammals/Cats/cats/cute-kitty-animated-gif-26.gif" alt="">

</div>
   <% else %>
  <h1> What's new</h1>
</div>
<!-- <div class="container">

  <%# @posts.reverse.each do |post|%>

  <div class="row">
    <div class="col-md-12 col-sm-12">
      <div class="container content">
        <img class="image-post" src="<%= post.image %>" alt="">
        <p><a class="post-title" href="/post/<%=post.id%>"><%= post.title %></a></p>
        <p><%= post.content %></p>
             <%#= post.user.try(:name) %>
        <p><%#=post.created_at.strftime("%y/%m/%d")%></p>

      </div>
    </div>
  </div> -->
<!-- </div> -->





  <div class="col-md-4 mt-4">
            <div class="card profile-card-5">
                <div class="card-img-block">
                  <% @posts.reverse.each do |post|%>
                   <img class="card-img-top" src="<%= post.image %>" alt="">
                </div>
                    <div class="card-body pt-0">
                    <h5 class="card-title"><a href="/post/<%=post.id%>"><%= post.title %></a></h5>
                    <p class="card-text"><%= post.content %></p>
                    <a href="#" class="btn btn-primary"><%= post.user.try(:name) %></a>
                  </div>
                </div>
                <p class="mt-3 w-100 float-left text-center"><strong><%=post.created_at.strftime("%y/%m/%d")%></strong></p>
        </div>

        <% end %>
        <% end %>
















</body>
